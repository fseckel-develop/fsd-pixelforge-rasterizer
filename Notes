TODO: Connecting Light Types with default Meshes

TODO: Default ShaderProgram for unlit objects (-> light sources completely white)

What are Shader Storage Buffer Objects (SSBO)?

What are Uniform Buffer Objects (UBO)?

What is SSAO (Screen Space Ambient Occlusion)?

What is the Cook-Torrance model in lighting?



Idea: "Chaining" Setter Methods by having the object type as return type
SpotLight* spot = new SpotLight("Spot");
spot->SetPosition(vec3(0.0f, 0.0f, 0.0f))
    ->SetDirection(vec(0.0f, -1.0f, 0.0f))
    ->CutoffAngles(5.0f, 20.0f);